# requirements.txt
# Core dependencies for DRecall

# CLI & Core
typer>=0.9.0
pydantic>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
rich>=13.0.0
toml>=0.10.2

# Interactive TUI
textual>=0.47.0
prompt-toolkit>=3.0.0

# Normalization (swappable via INormalizer interface)
markitdown>=0.1.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# AI/ML (swappable via IClassifier, IVectorizer interfaces)
transformers>=4.35.0
torch>=2.0.0
sentence-transformers>=2.2.0

# Storage (swappable via IStorage interface)
sqlite-vec>=0.1.0
